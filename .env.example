# Server configuration
PORT=3849

# Optional: Set a custom JWT secret for session token signing.
# If omitted, the server will securely generate one at startup and persist it in jwt_secret.key
# JWT_SECRET=your_custom_secure_jwt_secret_here
